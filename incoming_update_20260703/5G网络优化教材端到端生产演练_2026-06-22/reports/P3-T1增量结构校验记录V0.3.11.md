# P3-T1增量结构校验记录V0.3.11

生成日期：2026-06-26  
校验对象：P3-T1受控补拆增量成果  
当前基线：课程能力图谱主数据V0.3.11  
校验定位：结构校验，不替代专业复核、媒体审查、数据安全审查、学生正文审查和真实教学验证。

## 1. 校验结论

P3-T1本轮增量样表通过结构校验。

结论状态：

`无阻断结构错误，可以进入内部评审`

但不得据此进入：

1. V0.3.12或下一版主数据合并。
2. 学生端页面生产。
3. 正式资源包生产。
4. 出版社平台挂接。
5. AI生产工具链模块拆解。

## 2. 校验文件

| 文件 | 行数 | 字段数 | 结论 |
| --- | ---: | ---: | --- |
| `P3-T1详细能力节点主数据增量样表V0.3.11.csv` | 7 | 34 | 通过 |
| `P3-T1关系增量样表V0.3.11.csv` | 43 | 15 | 通过 |
| `P3-T1资源活动评价挂接增量样表V0.3.11.csv` | 21 | 18 | 通过 |
| `P3-T1输入输出物治理增量记录V0.3.11.csv` | 11 | 10 | 通过 |
| `P3-T1风险治理扩展增量样表V0.4.csv` | 14 | 46 | 通过 |

参照基线：

| 基线文件 | 行数 | 字段数 |
| --- | ---: | ---: |
| `课程能力图谱节点主数据表V0.3.11.csv` | 188 | 34 |
| `课程能力图谱关系主数据表V0.3.11.csv` | 563 | 15 |
| `课程能力图谱资源活动评价挂接表V0.3.11.csv` | 363 | 18 |
| `课程能力图谱输入输出物治理清单V0.3.11.csv` | 159 | 10 |

## 3. 校验项

### 3.1 字段结构

结果：通过。

1. 节点增量样表字段数为34，与V0.3.11节点主数据一致。
2. 关系增量样表字段数为15，与V0.3.11关系主数据一致。
3. 资源活动评价挂接增量样表字段数为18，与V0.3.11挂接表一致。
4. 输入输出物治理增量记录字段数为10，与V0.3.11输入输出物治理清单一致。
5. V0.4风险治理扩展样表字段数为46，与当前V0.4风险治理扩展口径一致。
6. 复跑CSV解析未发现额外字段、错列或异常行。

### 3.2 ID唯一性

结果：通过。

1. 新增7个节点ID唯一，且未与V0.3.11既有188个节点冲突。
2. 新增43条关系ID唯一，且未与V0.3.11既有563条关系冲突。
3. 新增21条挂接ID唯一，且未与V0.3.11既有363条挂接冲突。
4. 新增11条输入输出物治理ID唯一，且未与V0.3.11既有159条记录冲突。
5. 新增14条V0.4风险治理记录ID唯一。

### 3.3 关系端点

结果：通过。

新增关系的起点和终点均能回溯到以下对象之一：

1. V0.3.11既有节点。
2. P3-T1本轮新增节点。
3. P3-T1本轮新增资源活动评价挂接对象。
4. 已存在的能力单元或任务级节点。

重点检查通过：

1. `P3-T1 -> P3T1-N01...P3T1-N07` 的任务拆分关系成立。
2. `P3T1-N01 -> ... -> P3T1-N07` 的内部递进关系成立。
3. P3-T1到P3-T2/P3-T3/P3-T4的入口支撑关系可回溯。
4. P3-T1到P1/P2/P5/P6的关系被限定为入口映射，不替代后续任务能力节点。
5. `P3T1-N01...P3T1-N07 -> AU-04` 的能力挂接关系成立。
6. 资源证据关系可回溯到本轮挂接对象。

### 3.4 资源活动评价挂接

结果：通过。

21条挂接均绑定到P3-T1本轮新增详细能力节点。覆盖情况：

1. N01覆盖任务情境、网管作用说明和作用归纳活动。
2. N02覆盖架构层次图、标注工作单和交互标注活动。
3. N03覆盖软硬件组成对照表和部署边界判断。
4. N04覆盖功能组件用途表和组件匹配活动。
5. N05覆盖仿真实训系统入口图和入口定位活动。
6. N06覆盖功能到任务映射表和学习路径路由活动。
7. N07覆盖权限边界说明卡和边界判断活动。

### 3.5 输入输出物路径

结果：通过。

11条输入输出物治理记录均指向可识别资产路径或本轮生成资产路径。

特别说明：

1. 原教材媒体和截图仍只作为审查对象，不视为正式可发布资源。
2. 本轮没有移动旧文件，也没有覆盖V0.3.11推荐主数据。
3. 结构校验记录和内部评审报告属于本轮过程资产。

### 3.6 V0.4风险治理字段

结果：通过。

14条V0.4风险治理记录均包含46个字段。

检查结果：

1. `field_schema_version` 均为 `V0.4`。
2. `overall_publish_gate_status` 均为 `not_ready`。
3. `professional_gate_status` 未写成 `passed`。
4. `media_gate_status` 未写成 `passed`。
5. 风险对象覆盖7个详细能力节点和7个关键资源/活动对象。
6. 网管架构、云服务层次、功能边界、仿真截图、任务映射、权限和数据范围均保留风险门禁。

## 4. 非阻断问题

### 4.1 专业门禁仍未通过

本轮结构校验不能证明NFV、TECS、IaaS/PaaS/SaaS、系统管理、自运维、智能运维、无线应用和公共应用等解释完全专业准确。

### 4.2 媒体门禁仍未通过

P3-T1涉及 `image95` 至 `image121`。本轮只是把相关对象列入媒体审查和重绘范围，没有逐张判断可保留、需重绘、需替换、需删除或仅内部分析。

### 4.3 权限和平台边界仍未通过

仿真实训系统、厂家网管、真实生产系统和出版社平台不能混写。当前只完成风险结构化记录，尚未完成权限边界和平台边界正式审查。

### 4.4 学生正文尚未生成

本轮是图谱主数据增量验证，不是教材正文。结构通过不等于学生已经能学懂，也不等于教师可以直接拿去上课。

## 5. 阶段建议

建议下一步形成 `P3-T1增量补拆内部评审报告V0.3.11.md`。
