# 课程能力图谱风险治理扩展表V0.4校验记录

生成日期：2026-06-25  
校验对象：`reports/课程能力图谱风险治理扩展表V0.4.csv`  
依据文件：`reports/课程能力图谱V0.4回填合并建议.md`、`reports/课程能力图谱字段规范V0.4字段清单.csv`  
校验结论：通过。扩展表可以作为V0.4风险治理扩展层的当前数据基础，但不代表专业复核、编辑复核、媒体审查、数据安全审核、平台接口验证或真实教学验证已经完成。

## 1. 生成范围

扩展表由三张V0.4字段回填样表合并形成：

| 来源文件 | 行数 | 说明 |
| --- | ---: | --- |
| `reports/P1-T3_V0.4字段回填样表.csv` | 22 | 覆盖投诉信息采集的数据安全、授权、服务表达和发布门禁 |
| `reports/P3-T3_V0.4字段回填样表.csv` | 28 | 覆盖参数检查的专业口径、媒体重绘和资源审查门禁 |
| `reports/P4-T2_V0.4字段回填样表.csv` | 55 | 覆盖深样章、学生正文、教师材料、评价、媒体和平台门禁 |
| 合计 | 105 | 生成 `reports/课程能力图谱风险治理扩展表V0.4.csv` |

扩展表保留原46列结构：

```text
7个定位字段 + 39个V0.4字段
```

未新增 `edge_master` 或 `asset_governance` 记录，原因是本轮三任务样表只实际覆盖节点和资源活动评价挂接对象。

## 2. 结构统计

| 校验项 | 结果 |
| --- | ---: |
| 总行数 | 105 |
| 总列数 | 46 |
| `backfill_record_id`重复数 | 0 |
| 缺失外键数 | 0 |
| 定位字段或R1字段空值数 | 0 |
| 非法枚举值数 | 0 |
| `overall_publish_gate_status=approved` | 0 |
| `teaching_validation_gate_status=passed` | 0 |
| `platform_gate_status=passed` | 0 |

## 3. 来源表分布

| `source_table` | 行数 | 回挂对象 |
| --- | ---: | --- |
| `node_master` | 19 | 回挂 `课程能力图谱节点主数据表V0.3.3.csv` 的 `node_id` |
| `binding_master` | 86 | 回挂 `课程能力图谱资源活动评价挂接表V0.3.3.csv` 的 `binding_id` |

校验结果：19条节点记录均能回溯到V0.3.3节点主数据，86条挂接记录均能回溯到V0.3.3资源活动评价挂接表。

## 4. 任务分布

| 任务 | 行数 |
| --- | ---: |
| P1-T3 | 22 |
| P3-T3 | 28 |
| P4-T2 | 55 |

该分布与三张源样表一致。

## 5. 对象层级分布

| `object_level` | 行数 |
| --- | ---: |
| `detail_capability` | 19 |
| `resource_card` | 48 |
| `assessment_output` | 19 |
| `media_asset_review` | 12 |
| `overall_prototype_resource` | 7 |

该分布说明本轮V0.4风险治理扩展层已经覆盖详细能力节点、资源卡、评价产出、媒体审查对象和整体原型资源，但尚未覆盖关系对象和输入输出物治理对象。

## 6. 门禁状态

| 字段 | 分布 | 判断 |
| --- | --- | --- |
| `overall_publish_gate_status` | `not_ready` 105 | 符合当前阶段，未误标为可发布 |
| `teaching_validation_gate_status` | 无 `passed` | 符合当前未完成真实教学验证的事实 |
| `platform_gate_status` | 无 `passed` | 符合当前未完成出版社平台接口验证的事实 |

结论：扩展表没有把内部试验对象误写成可发布、已教学验证或已平台验证状态。

## 7. 字段清单补正记录

严格枚举校验时发现：`personal_info_type` 在无个人信息对象上使用 `not_applicable`，但原字段清单允许值未包含该值。该问题属于字段规范口径缺口，不属于扩展表合并错误。

已同步修订：

`reports/课程能力图谱字段规范V0.4字段清单.csv`

修订内容：

1. `personal_info_type` 的允许值增加 `not_applicable`；
2. 填写规则补充“无个人信息时填写 `not_applicable`”；
3. 备注补充“若 `contains_personal_info` 为 `yes` 则必须填写具体类型；若为 `no` 或 `not_applicable` 则填写 `not_applicable`”。

修订后重新校验，非法枚举值数为0。

## 8. 校验结论

`reports/课程能力图谱风险治理扩展表V0.4.csv` 通过当前结构校验，可以作为V0.4风险治理扩展层的当前数据基础。

成立的结论：

1. 扩展表可解析，字段完整；
2. 三任务共105行全部纳入；
3. 扩展表能通过外键回挂V0.3.3节点和挂接对象；
4. R1字段完整；
5. 枚举值合法；
6. 发布、教学验证和平台门禁保持保守；
7. V0.4字段清单已补正 `personal_info_type=not_applicable` 的口径缺口。

仍不成立的结论：

1. 不能说明内容已可发布；
2. 不能说明专业复核已完成；
3. 不能说明媒体资源已可直接使用；
4. 不能说明出版社平台接口已完成；
5. 不能说明真实一线教师/学生试看已经通过；
6. 不能直接进入页面生产、正式资源包生产或工具链模块拆解。

## 9. 下一步建议

建议下一步形成：

`reports/课程能力图谱风险治理扩展表V0.4短内部复核记录.md`

复核重点：

1. 旁挂式扩展表是否能稳定支撑后续任务增量；
2. 是否需要为 `edge_master` 或 `asset_governance` 设计后续补录规则；
3. `gate_blocking_reason` 是否需要从通用表述升级为更细的阻断原因枚举；
4. 是否可以在短内部复核通过后选择第四类任务做增量验证；
5. 是否仍应继续禁止页面生产和工具链模块拆解。
