# 课程能力图谱V0.3.5结构校验记录

生成日期：2026-06-25  
校验对象：V0.3.5候选主数据  
记录性质：内部结构校验记录，不是专家定稿、正式出版或平台发布验收

## 1. 校验结论

结论：通过。

V0.3.5候选主数据已通过结构校验。该结论只表示节点、关系、挂接、输入输出物治理和P4-T1风险治理旁挂门禁未发现结构性阻断错误，不表示专业复核、媒体审查、学生正文、教学验证、数据安全审查或平台接口验证已经完成。

## 2. 数据规模

| 主表 | V0.3.4 | 本次增量 | V0.3.5候选 |
| --- | ---: | ---: | ---: |
| 节点主数据表 | 139 | 6 | 145 |
| 关系主数据表 | 304 | 24 | 328 |
| 资源活动评价挂接表 | 214 | 18 | 232 |
| 输入输出物治理清单 | 57 | 16 | 73 |

说明：输入输出物治理清单本次增量16条，由P4-T1 10条输入输出物治理记录和V0.3.5自身6条输出记录组成。

## 3. P4-T1关系处理

本次合并只追加P4-T1增量关系，不改写V0.3.4既有关系，不提前追加P4-T3详细关系。

P4-T1新增关系类型分布：

| 关系类型 | 数量 |
| --- | ---: |
| 任务拆分详细节点 | 6 |
| 直接递进 | 6 |
| 支撑判断 | 5 |
| 问题回流 | 1 |
| 能力挂接 | 6 |

P4-T1新增关系中，19条为内部试拆，5条为待专业复核。待专业复核关系可以进入候选主数据，但不能写成专家已确认关系。

## 4. 校验项目

| 校验项 | 结果 | 说明 |
| --- | --- | --- |
| 字段结构一致性 | 通过 | V0.3.4主表和P4-T1增量表字段顺序一致。 |
| 节点ID唯一性 | 通过 | 未发现重复 `node_id`。 |
| 关系ID唯一性 | 通过 | 未发现重复 `v03_edge_id`。 |
| 挂接ID唯一性 | 通过 | 未发现重复 `binding_id`。 |
| 资产ID唯一性 | 通过 | 未发现重复 `asset_id`。 |
| 关系端点可回溯 | 通过 | 所有 `source_node_id` 和 `target_node_id` 均存在于V0.3.5候选节点表。 |
| 挂接节点可回溯 | 通过 | 所有资源活动评价挂接均可回溯到V0.3.5候选节点表。 |
| 输入输出物路径可回溯 | 通过 | 治理清单中的绝对路径均可解析；非绝对图片名作为同一媒体来源字段内的媒体标签处理。 |
| P4-T1风险治理旁挂门禁 | 通过 | P4-T1风险治理扩展记录12条，继续旁挂，不并入四张主表。 |
| 发布门禁保守性 | 通过 | P4-T1风险记录 `overall_publish_gate_status` 均为 `not_ready`，未误标为可发布。 |

校验过程中，`A-V035-006` 对应的校验记录路径在文件写入前暂时不存在；本文件写入后复查应闭合。该情况不构成数据结构错误。

## 5. P4-T1节点审核状态

P4-T1新增6个详细能力节点的审核状态分布如下：

| 审核状态 | 数量 |
| --- | ---: |
| pending_professional_review | 5 |
| pending_media_review | 1 |

风险等级分布如下：

| 风险等级 | 数量 |
| --- | ---: |
| high | 4 |
| medium | 2 |

这说明P4-T1虽然可进入候选主数据，但仍是待外部复核的内部候选状态。

## 6. 资源活动评价挂接审核状态

P4-T1新增18条资源活动评价挂接的类型分布如下：

| 挂接类型 | 数量 |
| --- | ---: |
| resource_card | 8 |
| assessment_output | 6 |
| learning_activity | 3 |
| media_asset_review | 1 |

挂接审核状态分布如下：

| 审核状态 | 数量 |
| --- | ---: |
| pending_professional_review | 10 |
| pending_teaching_validation | 4 |
| pending_editorial_review | 3 |
| pending_media_review | 1 |

这些挂接进入候选主数据只表示图谱连接关系成立，不表示资源已经可以正式生产或发布。

## 7. 风险治理边界

P4-T1的12条V0.4风险治理扩展记录继续作为旁挂风险层。本次不并入四张V0.3.5主表，也不并入全局 `reports/课程能力图谱风险治理扩展表V0.4.csv`。

当前保留以下观察项：

1. 工程授权和参数变更责任字段仍可作为后续V0.5字段升级观察项；
2. `binding_master` 风险记录中部分 `source_record_id` 使用资源或评价对象ID，可回溯到挂接表的 `binding_target_id`，但后续若并入全局风险治理扩展表，应统一引用口径；
3. 仿真软件截图、CSI-RS案例、变更记录模板和复测交接清单不能直接进入正式学生端资源。

## 8. 边界说明

1. V0.3.5当前是候选主数据，不是专家定稿课程能力图谱。
2. 本次只合并P4-T1增量，不改写V0.3.4既有节点、关系和挂接。
3. 本次不提前追加P4-T3详细关系。
4. P4-T1风险治理扩展记录继续作为旁挂风险层，不直接写入四张主表。
5. 软件截图、参数依据、工程授权、变更记录、CSI-RS案例和复测交接仍需专业复核、媒体治理、数据安全审查和教学验证。
6. V0.3.5结构校验通过后，也不应直接进入页面生产、资源包生产、平台发布或工具链模块拆解。

## 9. 后续建议

建议先进行一次V0.3.5短内部复核，确认是否把V0.3.5候选主数据作为新的推荐主数据基线。短复核通过前，不选择下一轮普通任务候选，不进入页面生产、资源包生产、平台发布或工具链模块拆解。
