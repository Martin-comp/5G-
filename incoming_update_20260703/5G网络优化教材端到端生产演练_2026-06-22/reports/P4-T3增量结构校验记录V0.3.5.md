# P4-T3增量结构校验记录V0.3.5

生成日期：2026-06-25  
校验对象：

1. `reports/P4-T3详细能力节点主数据增量样表V0.3.5.csv`
2. `reports/P4-T3关系增量样表V0.3.5.csv`
3. `reports/P4-T3资源活动评价挂接增量样表V0.3.5.csv`
4. `reports/P4-T3输入输出物治理增量记录V0.3.5.csv`
5. `reports/P4-T3风险治理扩展增量样表V0.4.csv`

## 1. 结构规模

| 文件 | 行数 | 字段数 | 结论 |
| --- | ---: | ---: | --- |
| P4-T3详细能力节点主数据增量样表V0.3.5.csv | 6 | 34 | 通过 |
| P4-T3关系增量样表V0.3.5.csv | 30 | 15 | 通过 |
| P4-T3资源活动评价挂接增量样表V0.3.5.csv | 19 | 18 | 通过 |
| P4-T3输入输出物治理增量记录V0.3.5.csv | 13 | 10 | 通过 |
| P4-T3风险治理扩展增量样表V0.4.csv | 12 | 46 | 通过 |

## 2. ID唯一性校验

| 对象 | ID字段 | 结果 |
| --- | --- | --- |
| 节点增量 | `node_id` | 无重复 |
| 关系增量 | `v03_edge_id` | 无重复 |
| 挂接增量 | `binding_id` | 无重复 |
| 输入输出物治理 | `asset_id` | 无重复 |
| 风险治理扩展 | `backfill_record_id` | 无重复 |

## 3. 主数据冲突校验

校验范围：

1. 既有主数据：`reports/课程能力图谱节点主数据表V0.3.5.csv`
2. 本次新增候选节点：`P4T3-N01` 至 `P4T3-N06`

校验结论：

1. 6个新增节点ID均未与V0.3.5既有节点冲突。
2. P4-T3在V0.3.5中仍为任务级骨架节点，本轮6个节点仅存在于增量样表中。
3. 未发现候选节点被误写入V0.3.5主表的情况。

## 4. 关系端点校验

30条关系均通过端点校验。

校验结论：

1. 30条关系均可找到起点。
2. 30条关系均可找到终点。
3. `P4-T3`、`P4T1-N05`、`P4T1-N06`、`P4T2-N02`、`P4T2-N06`、`P4T2-N07`、`P4T2-N08`、`P2T3-N08`、`AU-06`、`AU-07`、`AU-08` 均已存在于V0.3.5主数据或本次新增节点集合中。
4. 关系类型覆盖任务拆分、直接递进、前置支撑、问题回流和能力挂接。

## 5. 挂接节点校验

19条资源、活动、评价挂接均绑定到本次新增的6个详细能力节点。

校验结论：

1. 无缺失挂接节点。
2. 每个节点至少有3条挂接。
3. 高风险对象已进入V0.4风险治理扩展增量表，包括报告证据链清单、报告结构草稿、指标对比结论段、典型案例分析段、原教材测试截图组和报告沟通提纲。

## 6. 风险治理引用校验

12条V0.4风险治理记录均能回溯到本次新增节点或挂接目标。

校验结论：

1. `P4T3-N01` 至 `P4T3-N06` 均有节点级风险记录。
2. `P4T3-W02`、`P4T3-W03`、`P4T3-W04`、`P4T3-W05`、`P4T3-IMG01`、`P4T3-W06` 均有对象级风险记录。
3. 未发现孤立风险记录。
4. 所有风险记录保持 `not_ready` 或待审门禁状态，没有写成可发布状态。

## 7. 输入输出物路径校验

13条输入输出物治理记录中的文件路径均存在。

需注意：

1. `A-V035-INC-P4T3-008` 使用整本教材抽取文本记录P4-T3原文来源，具体范围为第7053-7365行。
2. `A-V035-INC-P4T3-010` 和 `A-V035-INC-P4T3-011` 指向原教材测试截图，只作为待审媒体来源。
3. 本校验记录和内部评审报告已补入输入输出物治理记录。

## 8. 校验过程记录

第一次校验发现两类结构错误：

1. 节点样表中 `P4T3-N02` 少1个字段，原因是 `source_type` 与 `source_ref_id` 未正确分列。
2. 关系样表中7条跨任务支撑关系少1个 `relation_type` 字段。

已修正后复验，结果通过。

## 9. 校验结论

P4-T3 V0.3.5增量结构校验通过。

当前可进入内部评审，但不建议直接合并主数据，原因如下：

1. P4-T3涉及报告输出、客户沟通和外部发布边界，仍需专业和编辑复核。
2. 覆盖方案对比表中的指标结论需通信专业教师复核。
3. 原教材 `image300.png`、`image301.png` 需媒体审查、脱敏或重绘。
4. 本轮只验证主数据增量结构成立，不代表学生正文、页面呈现、平台接口或真实教学验证已完成。
