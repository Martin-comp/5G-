# P6项目V0.4补齐结构校验记录

生成日期：2026-06-26  
校验对象：P6-T2风险治理补齐样表、P6项目内部关系校正清单  
前置计划：`reports/P6项目内部链条V0.4补齐计划.md`  
校验定位：结构校验，不替代通信专业复核、媒体审查、学生正文审查、平台验证和真实教学验证。

## 1. 校验结论

P6项目V0.4补齐样表通过结构校验。

结论状态：

`无阻断结构错误，可以进入P6项目V0.4补齐内部评审`

不得据此进入：

1. 第十类普通任务候选选择；
2. P6学生端页面生产；
3. 正式资源包生产；
4. 出版社平台挂接；
5. 工具链模块拆解。

## 2. 校验范围

| 文件 | 行数 | 字段数 | 结论 |
| --- | ---: | ---: | --- |
| `P6-T2_V0.4风险治理补齐样表.csv` | 32 | 46 | 通过 |
| `P6项目内部关系校正清单V0.4.csv` | 11 | 12 | 通过 |

## 3. 校验项

### 3.1 P6-T2风险治理补齐样表

结果：通过。

校验内容：

1. 记录数为32条；
2. 字段数为46列；
3. `backfill_record_id` 无重复；
4. `field_schema_version` 均为 `V0.4`；
5. `source_table` 仅使用 `node_master` 和 `binding_master`；
6. `task_id` 均为 `P6-T2`；
7. 7条 `node_master` 记录均可回溯到V0.3.9节点主数据；
8. 25条 `binding_master` 记录均可回溯到V0.3.9资源活动评价挂接表；
9. 发布、专业、媒体、学生正文、平台和教学验证门禁未误写成通过或发布就绪。

### 3.2 P6项目内部关系校正清单

结果：通过。

校验内容：

1. 记录数为11条；
2. 字段数为12列；
3. `relation_review_id` 无重复；
4. 11条必须纳入校正的关系均已覆盖；
5. `source_node_id` 和 `target_node_id` 均可回溯到V0.3.9节点主数据；
6. `source_relation_id` 均可回溯到V0.3.9关系主数据；
7. `chain_role` 枚举值无越界；
8. `visibility_scope` 枚举值无越界。

## 4. 校正覆盖关系

关系校正清单已覆盖：

1. `P6-T1 -> P6T2-N01`
2. `P6-T1 -> P6T2-N03`
3. `P6-T1 -> P6T2-N04`
4. `P6T1-N02 -> P6T2-N03`
5. `P6T1-N03 -> P6T2-N03`
6. `P6T1-N04 -> P6T2-N03`
7. `P6T1-N05 -> P6T2-N02`
8. `P6T1-N06 -> P6T2-N04`
9. `P6T1-N07 -> P6T2-N01`
10. `P6T1-N08 -> P6T2-N02`
11. `P6T1-N08 -> P6T2-N07`

其中前三条任务级旧关系被标注为项目总览层关系，不作为学生逐步学习路径的唯一入口；后八条详细节点关系进入学生路径、教师复核或过渡边界说明。

## 5. 保留问题

结构校验通过不代表以下事项完成：

1. P6-T2信令问题分类和排查链专业正确；
2. P6-T2涉及的日志、信令文件或案例数据已完成数据安全审查；
3. P6-T2媒体资源已经完成重绘、标注或替换；
4. P6-T2学生端教材正文已经生成并通过审读；
5. P6-T1到P6-T2的关系已经通过通信专业教师确认；
6. `P6T1-N08` 的过渡边界已经在学生正文和教师材料中落地；
7. 一线教师和学生真实试看已经完成。

## 6. 下一步建议

建议下一步形成：

`reports/P6项目V0.4补齐内部评审报告.md`

内部评审应判断：

1. P6-T2风险治理是否已经补到与P6-T1同一口径；
2. 任务级旧关系和详细节点级新关系是否已经分层；
3. `P6T1-N08` 是否被约束为过渡节点；
4. 项目六内部链条是否足以进入第十类普通任务候选选择。

在内部评审形成前，不选择第十类普通任务，不做P6学生端页面、正式资源包、出版社平台挂接或工具链模块拆解。
