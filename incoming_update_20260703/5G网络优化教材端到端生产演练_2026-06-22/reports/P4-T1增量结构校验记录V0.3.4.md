# P4-T1增量结构校验记录V0.3.4

生成日期：2026-06-25  
校验对象：

1. `reports/P4-T1详细能力节点主数据增量样表V0.3.4.csv`
2. `reports/P4-T1关系增量样表V0.3.4.csv`
3. `reports/P4-T1资源活动评价挂接增量样表V0.3.4.csv`
4. `reports/P4-T1输入输出物治理增量记录V0.3.4.csv`
5. `reports/P4-T1风险治理扩展增量样表V0.4.csv`

## 1. 结构规模

| 文件 | 行数 | 字段数 | 结论 |
| --- | ---: | ---: | --- |
| P4-T1详细能力节点主数据增量样表V0.3.4.csv | 6 | 34 | 通过 |
| P4-T1关系增量样表V0.3.4.csv | 24 | 15 | 通过 |
| P4-T1资源活动评价挂接增量样表V0.3.4.csv | 18 | 18 | 通过 |
| P4-T1输入输出物治理增量记录V0.3.4.csv | 10 | 10 | 通过 |
| P4-T1风险治理扩展增量样表V0.4.csv | 12 | 46 | 通过 |

## 2. ID唯一性校验

| 对象 | ID字段 | 结果 |
| --- | --- | --- |
| 节点增量 | `node_id` | 无重复 |
| 关系增量 | `v03_edge_id` | 无重复 |
| 挂接增量 | `binding_id` | 无重复 |
| 输入输出物治理 | `asset_id` | 无重复 |
| 风险治理扩展 | `backfill_record_id` | 无重复 |

## 3. 关系端点校验

校验范围：

1. 既有主数据：`reports/课程能力图谱节点主数据表V0.3.4.csv`
2. 本次新增节点：`P4T1-N01` 至 `P4T1-N06`

校验结论：

1. 24条关系均可找到起点；
2. 24条关系均可找到终点；
3. 未发现缺失关系端点；
4. `P2T3-N07`、`P3T3-N06`、`P4T2-N01`、`AU-05`、`AU-06`、`AU-07` 均已存在于V0.3.4主数据或本次新增节点集合中。

## 4. 挂接节点校验

18条资源、活动、评价挂接均绑定到本次新增的6个详细能力节点。

校验结论：

1. 无缺失挂接节点；
2. 每个节点均至少有2条挂接；
3. 高风险对象已经进入V0.4风险治理扩展增量表，包括仿真软件截图组、CSI-RS案例材料、责任边界表、优化实施变更记录模板、复测验证交接清单和参数依据解释卡。

## 5. 输入输出物路径校验

10条输入输出物治理记录中，已生成文件路径和原始输入路径均可追溯。

需注意：

1. `A-V034-INC-P4T1-009` 使用整本教材抽取文本记录P4-T1原文来源，具体范围为第5188-5800行；
2. `A-V034-INC-P4T1-010` 使用媒体池目录记录P4-T1相关图片来源，实际涉及图片包括天线、无线、承载、核心网基础图以及仿真软件截图；
3. 媒体图片当前只作为待审来源，不代表正式教材可直接使用。

## 6. V0.4枚举校验

对 `reports/P4-T1风险治理扩展增量样表V0.4.csv` 中的V0.4字段执行枚举校验。

校验依据：

`reports/课程能力图谱字段规范V0.4字段清单.csv`

校验结论：

1. `field_schema_version` 均为 `V0.4`；
2. `review_required_type` 枚举值有效；
3. `professional_risk_type` 枚举值有效；
4. `review_owner_role` 枚举值有效；
5. 数据安全、授权、媒体、发布、平台和教学验证门禁状态枚举值有效；
6. 未发现枚举越界。

## 7. 校验结论

P4-T1 V0.3.4增量结构校验通过。

当前可进入内部评审，但不建议直接合并主数据，原因如下：

1. P4-T1涉及真实网络优化实施和参数变更，工程授权边界仍需专业复核；
2. 原教材38张图片和多张仿真软件截图需媒体审查或重绘；
3. CSI-RS案例的参数逻辑、信令解释和指标变化需通信专业教师复核；
4. 本轮只验证主数据增量结构成立，不代表学生正文、页面呈现、平台接口或真实教学验证已完成。
