# P2-T1增量结构校验记录V0.4

生成日期：2026-06-25  
校验对象：

1. `reports/P2-T1详细能力节点主数据增量样表V0.4.csv`
2. `reports/P2-T1关系增量样表V0.4.csv`
3. `reports/P2-T1资源活动评价挂接增量样表V0.4.csv`
4. `reports/P2-T1输入输出物治理增量记录V0.4.csv`
5. `reports/P2-T1风险治理扩展增量样表V0.4.csv`

## 1. 结构规模

| 文件 | 行数 | 字段数 | 结论 |
| --- | ---: | ---: | --- |
| P2-T1详细能力节点主数据增量样表V0.4.csv | 7 | 34 | 通过 |
| P2-T1关系增量样表V0.4.csv | 32 | 15 | 通过 |
| P2-T1资源活动评价挂接增量样表V0.4.csv | 21 | 18 | 通过 |
| P2-T1输入输出物治理增量记录V0.4.csv | 8 | 10 | 通过 |
| P2-T1风险治理扩展增量样表V0.4.csv | 14 | 46 | 通过 |

## 2. ID唯一性校验

| 对象 | ID字段 | 结果 |
| --- | --- | --- |
| 节点增量 | `node_id` | 无重复 |
| 关系增量 | `v03_edge_id` | 无重复 |
| 挂接增量 | `binding_id` | 无重复 |
| 输入输出物治理 | `asset_id` | 无重复 |
| 风险治理扩展 | `backfill_record_id` | 无重复 |

## 3. 关系端点校验

校验范围：

1. 既有主数据：`reports/课程能力图谱节点主数据表V0.3.3.csv`
2. 本次新增节点：`P2T1-N01` 至 `P2T1-N07`

校验结论：

1. 32条关系均可找到起点；
2. 32条关系均可找到终点；
3. 未发现缺失关系端点；
4. `P1T3-N02`、`P1T3-N03`、`P2T2-N01`、`P2T2-N02`、`P2T3-N01`、`P2T3-N02`、`P4T2-N01`、`P5T3-N02`、`AU-02` 均已存在于V0.3.3主数据或前序合并主数据中。

## 4. 挂接节点校验

21条资源、活动、评价挂接均绑定到本次新增的7个详细能力节点。

校验结论：

1. 无缺失挂接节点；
2. 每个节点均至少有2条挂接；
3. 高风险对象已经进入V0.4风险治理扩展增量表，包括软件截图、入场许可、路线规划、DT异常记录、CQT记录和测试资料包。

## 5. 输入输出物路径校验

8条输入输出物治理记录中，已生成文件路径和原始输入路径均可追溯。

需注意：

1. `A-V04-INC-P2T1-008` 使用一个媒体池入口记录P2-T1相关图片，实际图片包括 `image74.png` 至 `image82.png`；
2. 媒体图片当前只作为待审来源，不代表正式教材可直接使用。

## 6. V0.4枚举校验

对 `reports/P2-T1风险治理扩展增量样表V0.4.csv` 中的V0.4字段执行枚举校验。

校验依据：

`reports/课程能力图谱字段规范V0.4字段清单.csv`

校验结论：

1. `field_schema_version` 均为 `V0.4`；
2. `review_required_type` 枚举值有效；
3. `professional_risk_type` 枚举值有效；
4. `review_owner_role` 枚举值有效；
5. 数据安全、授权、媒体、发布、平台和教学验证门禁状态枚举值有效；
6. 未发现枚举越界。

## 7. 校验结论

P2-T1 V0.4增量结构校验通过。

当前可进入内部评审，但不建议直接合并为V0.3.4，原因如下：

1. 7个节点的职业任务颗粒度需要进一步内部评审；
2. 软件截图、路线图和测试界面必须经过媒体审查或重绘；
3. 路测和CQT测试涉及实训安全、位置数据、测试卡和现场授权，仍需专业、数据安全和教学验证门禁；
4. V0.4字段目前能覆盖主要风险，但“实训安全”和“位置数据”是否需要单独升级为更细字段，仍需后续评审。
